server:
  port: 8083
spring:
  datasource:
    url: jdbc:postgresql://localhost:45432/inventory
    username: inventory
    password: changeme
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      platform: postgres
  security:
    user:
      name: admin
      password: password
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml
    default-schema: inventory
    liquibase-schema: inventory
    database-change-log-table: databasechangelog
    database-change-log-lock-table: databasechangeloglock


catalog-service:
  url: "http://localhost:8081"

security:
  jwt:
    public-key-path: classpath:public.pem
